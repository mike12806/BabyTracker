{
  "name": "baby-tracker-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:seed": "wrangler d1 execute baby-tracker-db --local --file=seed/seed.sql"
  },
  "dependencies": {
    "hono": "4.12.23"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.16.10",
    "@cloudflare/workers-types": "4.20260527.1",
    "typescript": "6.0.3",
    "vitest": "4.1.7",
    "wrangler": "4.95.0"
  }
}
