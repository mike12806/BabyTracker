/// <reference types="vite/client" />

/** Short commit the running bundle was built from — see `define` in vite.config.ts. */
declare const __BUILD_ID__: string;

/** ISO timestamp of when the running bundle was built. */
declare const __BUILD_TIME__: string;
